# Database - Local PostgreSQL
DATABASE_URL="postgresql://postgres:password@localhost:5432/ecommerce_db"

# JWT
JWT_SECRET="super-secret-jwt-key-change-in-production-2024"

# Server
PORT=3001
NODE_ENV=development

# CORS - Local frontend URL
CORS_ORIGIN="http://localhost:3000"

# OpenAI (OPȚIONAL)
# OPENAI_API_KEY="sk-your-real-api-key-here"
OPENAI_MODEL="gpt-4o-mini"

# Email Configuration (SMTP) - CONFIGUREAZĂ PENTRU A TRIMITE EMAIL-URI REALE
# IMPORTANT: Dacă nu configurezi SMTP, codul de verificare va apărea în consola backend-ului!
# 
# Pentru Gmail:
# 1. Activează "2-Step Verification" în contul Google
# 2. Generează "App Password" la: https://myaccount.google.com/apppasswords
# 3. Folosește App Password-ul generat mai jos (NU parola ta normală)
#
# Pentru Yahoo:
# 1. Activează "2-Step Verification" 
# 2. Generează "App Password" la: https://login.yahoo.com/account/security
# 3. Folosește App Password-ul generat mai jos
#
# ALTERNATIVĂ SIMPLĂ - Mailtrap (RECOMANDAT pentru testare):
# 1. Cont gratuit: https://mailtrap.io/
# 2. Copiază credențialele SMTP din Mailtrap
# 3. Configurează mai jos:
#    SMTP_HOST="sandbox.smtp.mailtrap.io"
#    SMTP_PORT=2525
#    SMTP_USER="<din-mailtrap>"
#    SMTP_PASS="<din-mailtrap>"

SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER="your-email@gmail.com"              # ← SCHIMBĂ CU EMAIL-UL TĂU REAL!
SMTP_PASS="your-16-character-app-password"    # ← SCHIMBĂ CU APP PASSWORD REAL!
SMTP_FROM_EMAIL="noreply@example.com"
SMTP_FROM_NAME="E-Commerce App"